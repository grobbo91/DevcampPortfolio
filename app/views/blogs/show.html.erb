<p id="notice"><%= notice %></p>

<h2 class="blog-post-title"><%= @blog.title %></h2>
       <p class="blog-post-meta">Published <%= distance_of_time_in_words(@blog.created_at, Time.now) %> ago </p>
       	<p class="blog-post-meta"><%= link_to @blog.status, toggle_status_blog_path(@blog) if logged_in?(:site_admin) %> / <%= link_to 'Edit', edit_blog_path(@blog) if logged_in?(:site_admin) %></p>


       <p><%= @blog.body %> </p>


<%= link_to 'Back', blogs_path %>
